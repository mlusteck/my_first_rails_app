$("#comments").empty().prepend("<%= j render partial: 'products/comments' %>");
$('.comments-pagination a').attr('data-remote', 'true');
refreshRatyRating();
console.log("Listen to comments!");
console.log(App.product.listenToComments);
App.product.listenToComments();
setTimeout( function() {
  console.log("Listen after waiting!");
  App.product.listenToComments();
}, 500);
