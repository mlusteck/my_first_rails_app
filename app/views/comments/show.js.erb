$("#comments .product-reviews").prepend(
  "<%= j render partial: 'comment', locals: {comment: @comment, current_user: current_user, hidden: true} %>"
);
changeAverageRating("<%= @product.average_rating %>");
refreshRatyRating();
$("#<%= @comment.html_id %>").slideDown();
